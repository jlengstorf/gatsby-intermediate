---
title: Other React Stuff
---

import Button from '../../src/components/button';

HONK

```jsx react-live
() => {
  const handleClick = event => {
    event.preventDefault();

    alert('Clicked!');
  };

  return (
    <div>
      <h1>Look, a button!</h1>
      <Button text="Click Me" onClick={handleClick} />
    </div>
  );
};
```

```jsx
<SomeComponent />
```
